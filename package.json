{
  "name": "alpina-market",
  "private": true,
  "type": "module",
  "scripts": {
    "server": "json-server --watch db/db.json --port 3000",
    "server:host": "json-server --watch db/db.json --host 0.0.0.0 --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}
